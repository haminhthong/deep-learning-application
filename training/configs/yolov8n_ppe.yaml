# Configuration for YOLOv8n PPE Experiment (Training Contract)
experiment_name: yolov8n_ppe_v1
model_type: yolov8n.pt
data_config: training/data.yaml

# Hyperparameters passed to Ultralytics YOLO.train()
seed: 42
image_size: 640
epochs: 100
batch_size: 16
optimizer: auto
lr0: 0.01
lrf: 0.01

# Reference contracts
dataset_contract: configs/dataset_v1.yaml
runtime_policy: configs/runtime_policy.yaml
